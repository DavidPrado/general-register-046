spring:
  application:
    name: general-registration

  codec:
    max-in-memory-size: 10MB

  r2dbc:
    url: r2dbc:postgresql://localhost:5435/etec046_db
    username: etec046_root
    password: "etec046@!2025"
    pool:
      initial-size: 5
      max-size: 20
      max-idle-time: 30m

  boot:
    admin:
      client:
        url: http://localhost:9090
        username: admin@etec.sp.gov.com.br
        password: "Etec046@!"
        instance:
          metadata:
            user.name: ${spring.security.user.name}
            user.password: ${spring.security.user.password}

  security:
    user:
      name: admin-monitor
      password: "Etec046@password"
    oauth2:
      resourceserver:
        jwt:
          secret-key-spec: "kHbjdX0iUYFIyKc9FULjbOgtA5mwr8CfcfeLNCxuxyGFBrfZsuRAKqCwXlrUUsYO"
          jws-algorithm: HS512

server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics,loggers,logfile,threaddump,heapdump"
  endpoint:
    health:
      show-details: always
    loggers:
      enabled: true


logging:
  file:
    name: logs/general-registration.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7
  level:
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO
    org.springframework.web: INFO
    org.springframework.web.reactive: INFO